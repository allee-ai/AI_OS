# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Build outputs
dist/
build/
*.egg-info/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Environment files
.env
.env.local
.env.*.local

# VM deployment scripts (local-only)
scripts/connect_vm_aios.sh
scripts/install-vm.sh
scripts/sync_local
connect_vm_nola.sh

# *.command
create_vm_desktop_app.sh
*.app/

# API Keys & Secrets (CRITICAL - never commit!)
**/openai_key.txt
**/api_key.txt
**/*_key.txt
**/*_secret.txt
**/*.pem
**/*.key

# Logs
*.log
LOG.txt
logs/
!docs/logs/CHANGELOG.md
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids/
*.pid
*.seed
data/.last_state_read

# Database files
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
*.db-journal
data/db/*.db
data/db/*.db-*

# But allow demo database (ships pre-populated)
!data/db/state_demo.db

# But keep the data/db directory structure
!data/db/.gitkeep

# Test coverage
coverage/
.nyc_output/
.pytest_cache/

# Temporary files
tmp/
temp/
*.tmp

# OS files
Thumbs.db

# Conversation data exports (generated at runtime)
Feeds/conversations/

# Auto-generated training data
finetune/auto_generated/
finetune/runs/
finetune/*_train.jsonl
finetune/aios_combined.jsonl
finetune/train.jsonl
finetune/valid.jsonl
finetune/reasoning_train.jsonl
finetune/venv-mlx/
finetune/venv-cuda/
finetune/convo_chunks/
finetune/pretrain_data/

# Frontend build artifacts
frontend/dist-demo/

# Local evaluator checklist
docs/checklist.md

# Local archives (developer notes, old docs)
docs/_archive/

# Business plan (confidential)
docs/BUSINESS_PLAN.md

# Eval transcripts (generated at runtime)
eval/transcripts/*.json
eval/transcripts/*.md
# Build artifacts and generated files
*.dmg
*.icns
.aios_installed
vscode_import_log.json

# Development utility scripts (not needed for users)
stringify/_archive/

# Local archive (not tracked)
_archive/

# Adapter weights (large binary files)
finetune/adapters-*/
finetune/runs/*/adapters/*.safetensors
finetune/aios_base.jsonl
finetune/fused-behavioral/

# Experiment runs (large model weights + training logs)
experiments/runs/

# Hebbian attention data (generated matrices)
research/hebbian_attention/data/

# Keep generated training data in git (small JSONL)

# Website has its own repo (allee-ai/allee-ai.github.io)
workspace/allee-ai.github.io/
workspace/aios-demo
ELEUTHER_PITCH.md
