# Nola runtime configuration
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===========================================
# MODEL PROVIDER CONFIGURATION
# ===========================================
# Provider options: ollama | http | mock
NOLA_MODEL_PROVIDER=ollama
# Model name used when provider=ollama (qwen2.5:7b recommended for CPU)
NOLA_MODEL_NAME=qwen2.5:7b
# Optional HTTP endpoint when provider=http (expects POST with {messages: [...]})
NOLA_MODEL_ENDPOINT=

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Backend/Frontend ports
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Identity DB (optional overrides)
STATE_DB_PATH=./data/db/state.db

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ===========================================
# OPTIONAL API KEYS (for extended features)
# ===========================================
# Kernel Browser Automation (enables "living body" features)
# Get your API key from: https://app.onkernel.com
KERNEL_API_KEY=

# OpenAI (optional - for Elaris or fallback)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Linear Integration (optional - for task management)
# Get your API key from: https://linear.app/settings/api
LINEAR_API_KEY=