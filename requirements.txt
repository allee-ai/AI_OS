# AI_OS - Root Requirements
# AI OS — Python Dependencies
# Preferred: uv sync (uses pyproject.toml + uv.lock)
# Fallback:  pip install -r requirements.txt

# Core backend
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
starlette>=0.27.0
python-dotenv>=1.0.0
aiofiles>=23.2.1

# HTML/JSON parsing for imports
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Numerical (for subconscious/embeddings)
numpy>=1.24.0

# Ollama client (local LLM)
ollama>=0.1.0

# Optional: Kernel browser automation (for "living body" features)
# Install with: pip install -e ".[automation]"
# kernel>=0.26.0
# playwright>=1.57.0

# Optional: Encryption for secrets (falls back to base64 without it)
# Install with: pip install -e ".[encryption]"
# cryptography>=41.0.0

# vCard parsing for contacts import
vobject>=0.9.6

# Web search (DuckDuckGo, no API key needed)
duckduckgo-search>=7.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
httpx>=0.25.0
